function main(){
    var x = get_int_s();

    var y = 15;

    var z = get_int_s();

    while (z != x) do {
        print_int(y);
        z = x;
    }

    while (y > 14) do {
        print_int(y);
        y = y - 1;
    }
    return; 
}