function main() {
    var a = 0;
    var b = 4;

    while a < b do {
        a = a + 1;
    }
    while b < a do {
        print_str("This should not run.");
    }

    while b == 4 do {
        print_str("this should only print once.");
        b = b - 1;
    }

    print_int(a);

    return ;
}